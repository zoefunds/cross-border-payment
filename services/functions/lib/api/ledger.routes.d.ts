/**
 * Ledger Routes
 * GET /ledger/history        — Get my ledger history
 * GET /ledger/balance/:currency — Get my wallet balance
 */
export declare const ledgerRouter: import("express-serve-static-core").Router;
