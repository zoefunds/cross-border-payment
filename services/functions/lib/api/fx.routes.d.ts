/**
 * FX Routes
 * GET /fx/rate/:pair     — Get current rate for a pair
 * GET /fx/quote          — Get a transaction quote
 */
export declare const fxRouter: import("express-serve-static-core").Router;
